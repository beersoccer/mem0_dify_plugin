---
globs: ["*.yaml", "manifest.yaml", "build_package.sh", ".difyignore"]
description: Dify plugin specific constraints and packaging rules
---

# Dify Plugin Constraints

## Source of Truth Files
- `manifest.yaml`: name/version/author/metadata
- `provider/*.yaml` or `tools/*.yaml`: tool/provider definitions
- `.difyignore`: packaging excludes
- `build_package.sh`: packaging script

## Plugin Configuration
- Do not invent manifest fields or plugin schema; read repo files first
- Always read `manifest.yaml`, `provider/*.yaml`, or `tools/*.yaml` for actual definitions

## Packaging
- Do not include secrets in repository or packaged artifacts
- If packaging fails (size/ignored files), review `.difyignore` first
- Prefer using documented Dify plugin packaging commands and structure
- Ensure `.difyignore` excludes: `.env*`, keys, local caches, tests
