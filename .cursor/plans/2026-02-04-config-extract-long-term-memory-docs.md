## Plan
- Review `CONFIG.md` for current usage and image references
- Add a configuration subsection for `extract_long_term_memory` with the new image
- Identify all `_example.png` references across docs
- Rename images in `images/` to drop `_example` suffix
- Update image references in `CONFIG.md` (and any other docs using them)
- Quick scan for broken links/typos in updated sections

## Files to touch
- `CONFIG.md`
- `README.md` (if it references renamed images)
- `images/*.png`

